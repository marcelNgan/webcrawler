#ifndef LINKSTORER_HPP
#define LINKSTORER_HPP
#include <string>
#include <list>

class linkstorer
{
    string url;
    list<string> links;
public:
    void printLink ();
    storer(string url);
    ~storer();
}
#endif
