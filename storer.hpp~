#ifndef LINKSTORER_HPP
#define LINKSTORER_HPP
#include <string>
#include <list>
#include <iostream>
#include <fstream>
class linkstorer
{
    string url;
    list<string> links;
public:
    void linksprinter ();
    storer(string url);
    ~storer();
}
#endif
